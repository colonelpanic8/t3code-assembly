# Agent instructions

- Read `BUILDING.md` completely before changing or publishing the assembly.
- Preserve dirty repository, submodule, and worktree state. Never work directly
  on the generated `t3code/assembled` branch.
- Commit `assembly.toml`, `assembly.lock.json`, and the `t3code/` submodule pin
  together.
