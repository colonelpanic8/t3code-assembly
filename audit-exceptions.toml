# Reviewed missing-line sets from intentional later rewrites.  The audit checks
# the exact count and SHA-256 of each (path, normalized line) set, so a newly
# missing line or a changed explanation set fails rather than being waived.

[[exception]]
entry = "3984"
missing_count = 29
missing_sha256 = "bb488993771671cbe411b2c8c5b7a7613caabc7c0632f86601633b220ac095b8"
reason = "The local artifact-safety topic rewrites the original image-path resolver and its tests around ThreadArtifactResolver; the supported generated and viewed image paths remain covered there."

[[exception]]
entry = "t3code/group/thread-picker"
missing_count = 2
missing_sha256 = "b1d83dfcf99f1ceaed163b4784c7ebed9093396f099c133e3061e62f0979c473"
reason = "Same reviewed loss as before, now reported exactly: the audit's count fell from 17 to 2 because this refresh rebuilt the group at its current member heads, so its normalization no longer inflates the set. Both remaining lines are in packages/shared/src/keybindings.ts and are the chat.newEnvironment (mod+shift+n) and project.add (alt+a) default rules. Re-verified directly against build f6c97d801: both COMMANDS survive in the contracts command list (packages/contracts/src/keybindings.ts:78 and :80) and stay invocable from the command palette; only their default shortcuts are absent, because the client-owned keybindings topic moves default rules into packages/contracts and deliberately reassigns mod+shift+n to chat.newLocal (:150). The same two lines are also waived per-member under 4258 and 4426; this entry is the group-level view of that one set, not additional loss."

[[exception]]
entry = "4271"
missing_count = 85
missing_sha256 = "8c270792dc2b09a0ce87e8659088a734d5ef986a8619cab6f693000d07f7bf9f"
reason = "Later selector and compact-composer work rewrites the shortcut-hint JSX around ComposerControlShortcutHint and the controlled BranchToolbar selectors. remainingShortcutLabelForCommand and shouldShowCommandHintForModifiers remain the shared behavior, their current tests cover held-modifier and glyph formatting, and all six defaults are carried in packages/contracts DEFAULT_KEYBINDINGS by the refreshed-client overlap epilogue."


[[exception]]
entry = "4323"
missing_count = 12
missing_sha256 = "b85298b9911197c64a74299b1fa20ee0b03395da54527e327e4dfa0a26111a56"
reason = "The cumulative client topic rewrites these call sites while retaining #4323's Cache-backed buffering, bounded coalescing, retry path, completion fallback, and duplicate-finalization guards. The final implementation keeps the stronger buffered-boundary behavior rather than the branch's exact helper-local expressions."

[[exception]]
entry = "4337"
missing_count = 1
missing_sha256 = "40be42ca3a30e5027178b136ffee2d9a8cce8a056440a9d508feed11bd0e09c4"
reason = "Project favicon resource construction moved to the asset-access request layer; project-favicon resources and revision-aware cache behavior remain covered by AssetAccess tests."

[[exception]]
entry = "4427"
missing_count = 5
missing_sha256 = "b42af86e8d50a6111f952480dc3aa2f3e4e2524b1182cb022fd188c12a860802"
reason = "Inline-image handling was folded into the safer ThreadArtifactResolver lookup and #4506's later timeline grouping. Generated-image work entries are still kept outside collapsed terminal groups, render through GeneratedImageWorkEntryLink, and expose the current expanded-row accessibility state."

[[exception]]
entry = "4437"
missing_count = 2
missing_sha256 = "7ecd88db4d7944e3e8a823b84856de7b7209f3fac6023f4129cc14d35a363673"
reason = "Diagnostics now reads storage from the selected environment's serverConfig instead of primaryServerStorageAtom. StoragePathRow remains, and the final selected-environment panel exposes configuration, data, state, cache, runtime, database, settings, keybindings, worktrees, and attachments paths with copy controls and an unavailable-storage fallback."


[[exception]]
entry = "4390"
missing_count = 1
missing_sha256 = "24f3654a881718c27f62c5df5f456eb06b2c04dc305e08b48fb2fd677bb2d53b"
reason = "The missing line only adds hasPersistedResumeCursor to ProviderRuntimeIngestion's local test harness. The final tree retains the production method and the ProviderCommandReactor coverage that exercises its fork-versus-resume decision; this ingestion harness does not call it."

[[exception]]
entry = "t3code/unify-environment-selection"
missing_count = 9
missing_sha256 = "70c57369673d3df71cdce1962c3ea435c8db93e031df501a0bf199bc301bd0c7"
reason = "The assembly retains #4323's coalesced, retry-safe assistant ingestion while making it unconditionally live for per-client presentation, replacing the topic's simpler direct-dispatch expressions while keeping stronger buffered-boundary and oversized-delta coverage. The settings line is replaced by useUpdateSettingsTarget's final client/server-owned signature."

[[exception]]
entry = "4263"
missing_count = 3
missing_sha256 = "18ffda5ecb1642af316c37f4087c789c739e9c90e9037c54641a3a4787fa7a0e"
reason = "The thread-picker group replaces the early Command Palette keyboard-navigation implementation with the consolidated picker flow, so these lines exist under different names in the final tree. Counts dropped from 10/7/7 to 3/3/4 in this refresh because the stale t3code-command-palette-logic-helpers group epilogue was removed -- it had been DELETING shouldHandleCommandPaletteShortcut and shouldClearAddProjectEnvironmentOnPop and stripping isLoaded from resolveNewThreadOnIntent, which is also what broke the web build with two MISSING_EXPORT errors. The remaining three lines are its addProjectActionItem construction, expressed through the group's consolidated action-item list."

[[exception]]
entry = "4258"
missing_count = 3
missing_sha256 = "d77cc0670ee8108980db9066877cf401b2825960268ce087e9cd6600549ac48e"
reason = "The thread-picker group replaces the early Command Palette keyboard-navigation implementation with the consolidated picker flow, so these lines exist under different names in the final tree. Counts dropped from 10/7/7 to 3/3/4 in this refresh because the stale t3code-command-palette-logic-helpers group epilogue was removed -- it had been DELETING shouldHandleCommandPaletteShortcut and shouldClearAddProjectEnvironmentOnPop and stripping isLoaded from resolveNewThreadOnIntent, which is also what broke the web build with two MISSING_EXPORT errors. The project.add COMMAND survives and stays invocable; only its alt+a default rule is not carried into packages/contracts."

[[exception]]
entry = "4426"
missing_count = 4
missing_sha256 = "fd20f0bdf92304b453745e554746032f666d00aadf467a507a87cbe427fca840"
reason = "The thread-picker group replaces the early Command Palette keyboard-navigation implementation with the consolidated picker flow, so these lines exist under different names in the final tree. Counts dropped from 10/7/7 to 3/3/4 in this refresh because the stale t3code-command-palette-logic-helpers group epilogue was removed -- it had been DELETING shouldHandleCommandPaletteShortcut and shouldClearAddProjectEnvironmentOnPop and stripping isLoaded from resolveNewThreadOnIntent, which is also what broke the web build with two MISSING_EXPORT errors. The chat.newEnvironment COMMAND survives; the client-owned keybindings topic reassigns mod+shift+n to chat.newLocal instead of carrying its default rule."

[[exception]]
entry = "4599"
missing_count = 1
missing_sha256 = "5b869bc8d4ab700f1346bb290822905727c771430654893b0eb090c253528aa0"
reason = "The single missing line is the updated mod+f rule in packages/shared. The client-owned keybindings topic moves it into packages/contracts DEFAULT_KEYBINDINGS with the same !terminalFocus && !previewFocus condition, so find stays hidden while preview owns focus."

[[exception]]
entry = "4605"
missing_count = 1
missing_sha256 = "e8a11c5b6c0f7bb0fde42278132b06c01f7fb1fcbb3ea6ba9c2aa2d3a15b6856"
reason = "The exact Option.isNone snapshot condition is widened by #4405's recovery behavior: an empty cached message body also reloads its snapshot before replay resumes. The replay generation, completion marker, and batching behavior remain intact."

[[exception]]
entry = "t3code/mobile-thread-replay-render-fixes"
missing_count = 1
missing_sha256 = "654e16f3e6c205179fdb9ec9b3b26ea5672c0a18ffd44612a9c84f7935ad6772"
reason = "The three-argument haptics call is combined with the cumulative topic's client-owned enableAssistantStreaming flag. The final four-argument call suppresses haptics both when streaming presentation is disabled and during replay catch-up."

[[exception]]
entry = "4590"
missing_count = 5
missing_sha256 = "9c950914f5e77a7ee8db1e04a342b91cec4f7568fcc72e088adf2d91017f533f"
reason = "The group integration consolidates #4590's conditional empty-state JSX into resolveCommandPaletteEmptyStateMessage, so its five inline `emptyStateMessage:` lines exist under that helper instead. Verified against build 83e99218b: the browse-unavailable, choose-a-destination, relative-paths and create-this-folder messages are all present in the final Command Palette. The group was rebuilt at the current member heads in this refresh, so the earlier waiver -- which covered a 32-line unbuilt-revision gap from a stale group pin -- no longer applies and has been replaced."


[[exception]]
entry = "t3code/orchestration-v2-main/managed-fleet"
missing_count = 1
missing_sha256 = "8a6fb02a6dad8598fbe7ba8b9dea68142e9a3e9f90bcfd9557a38efbe34c00c8"
reason = "The one absent line is the `Config.ConfigError,` error-channel line of DesktopEnvironment.make's Effect.fn.Return signature. The built tree carries the same signature with the error widened into a union (`| Config.ConfigError | DesktopStorageDirectoryConfigurationConflictError | PlatformError.PlatformError`) by #4437's XDG storage layout, so the line is present with a leading `|`. The topic's FileSystem requirement, managedConnectionsPath field, and default-path discovery are all carried verbatim."

[[exception]]
entry = "4439"
missing_count = 1
missing_sha256 = "267b3f7a711a85e8fcb50ec07c6f07a8bbfb854686d9693c6e41a5a489df62a9"
reason = "The one absent line is the `settings.worktreePathTemplate,` memo dependency in the old combined restore hook. The client suite's useEnvironmentSettingsRestore depends on the whole `settings` object, so a per-key entry is redundant. The template's dirty label, reset, and the worktree-path row are all carried (the row and its search item through the suite's fixup)."

[[exception]]
entry = "4494"
missing_count = 1
missing_sha256 = "3bca6be31e54df7b2c20de7abc4c972d8792706244c0024ee091b1a4cbfc57d3"
reason = "The one absent line is useUpdateEnvironmentSettings reading settingsValueAtom; the client suite reads `configValueAtom(environmentId)?.settings` so the hook accepts a null environment id, and still hands the loaded server settings to #4494's two-argument serialized-write useUpdateSettingsTarget. The pendingServerSettings.test.ts fixture renames that used to be in this exception now live in the topic itself."

[[exception]]
entry = "t3code/orchestration-v2-main/client-environment-suite"
missing_count = 4
missing_sha256 = "c58a9ba4b07cd08b10601a97ac710ae03460e9f3a05206525bb836eebe29836f"
reason = "Three lines are config.ts deriving serverRuntimeStatePath through the shared deriveServerRuntimeStatePath helper; on this stack #4437's storage roots win and the path is `join(runtimeDir, \"server-runtime.json\")`, which is the same location in the legacy layout this install uses (the desktop discovery still reads the legacy path; split-layout discovery is noted on the entry). The fourth is a type-only import of PersistedServerRuntimeState that became a value import because #4420's strict startup reader decodes with the shared schema."

[[exception]]
entry = "4420"
missing_count = 3
missing_sha256 = "bd73142b1aa58611d2a6633cb3365ce2381ca8c9930f3fa8e0f87df748cbc3f5"
reason = "The three lines are #4420's re-export of readPersistedServerRuntimeState as a log-and-swallow wrapper around its strict startup reader. The client suite already re-exports the shared reader with identical semantics (every ServerRuntimeStateError logged and mapped to none), so the duplicate definition was dropped to avoid a conflicting export. The strict readPersistedServerRuntimeStateForStartup, decideServerRuntimeStartup, ensureExclusiveStateDir, and ServerStateDirConflictError are carried verbatim."
